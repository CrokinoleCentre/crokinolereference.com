The zip file includes all the necessary python scripts, inputs and base html files.

To run, execute python main script and static website is outputted to the working directory in a subfolder called crokinolereference.com.

The script inputchecks can be ran if there are modifications to the inputs to catch errors that may occur in the run.

The example input contains the file that's uploaded to website, as an example of what can be emailed to the web owner so that others can contribute results/matches to the database.}
